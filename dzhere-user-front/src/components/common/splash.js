import * as React from 'react';
import { Button, Text, TextInput, View } from 'react-native';

const splash = () => {
    console.log('splash...');
    return (
      <View>
        <Text>Loading...</Text>
      </View>
    );
};

export default splash;